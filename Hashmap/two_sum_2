hashmp={}
for i,n in enumerate(numbers):
		diff=target-n
		if diff in hashmp:
			return [diff[i]+1,i+1]
		hash[n]=i
#hashmp's time complexity= o(n) 
#what if no extra memory is needed 

i=0
j=len(numbers)-1
while i<=j:
	if numbers[i]+numbers[j]==target:
			return[i+1,j+1]
	elif numbers[i]+numbers[j]<target:
			i+=1
	else:
		j+=1
